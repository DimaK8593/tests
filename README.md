Autotests for Test Task 

1. Create a virtual environment 
```bash
python -m venv venv
venv\Scripts\activate  
pip install -r requirements.txt
playwright install


2. Set dependencies
pip install -r requirements.txt
playwright install
